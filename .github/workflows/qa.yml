name: QA Deployment

on:
  push:
    branches:
      - 'stage-qa'  # This will trigger on push to stage-qa
  pull_request:
    branches:
      - 'stage-qa'  # Uncomment if you want to run checks on PRs

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Use Node.js 18.x
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build

  deploy-qa:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/stage-qa'  # Change to match the branch
    environment: qa
    
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Vercel (QA)
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_QA_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./ 
          vercel-args: '--name qa-test-pipeline-project'
